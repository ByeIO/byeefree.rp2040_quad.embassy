[package]
name = "bye_rp2040_multi_uart_pio_rs"
version = "0.0.1"
edition = "2021"
authors = [ "qsbye" ]

[dependencies]
# 依赖embassy-rp库
embassy-rp = { version = "0.4.0", features = ["rp2040"] }
# 固定为0.2版本
pio-proc = "0.3.0"
pio = "0.3.0"
